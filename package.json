{
  "devDependencies": {
    "buffer": "^6.0.3",
    "concurrently": "^9.0.1",
    "process": "^0.11.10",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "json-server": "^0.17.4",
    "parcel": "^2.12.0"
  },
  "scripts": {
    "start:parcel": "npx parcel index.html",
    "start:db": "npx json-server -w db.json",
    "start": "concurrently npm:start:*"
  }
}
